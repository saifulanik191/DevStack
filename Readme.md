Name of my Project-----DevStack
Description-----This project is about a tech info web app where you can explore the stack and tools for the building web apps.see their features how the works and you can also add to your stack cart if your want
Technology Used-----To build this project I mainly used react libray with typescript and for the styling i used tailwiind css and in few daisyUI componnet and for extra help used tailwind intelegences ,snipping extension
3 features about project----1- It has beautyful and clean ui 2-Proper color braning 3-Fully functional


1-What is JSX, and why is it used in React?
Ans-----Jsx is a extended feature of js where we can write HTML code and react use to to make better UI

2-What is the difference between props and state?
Ans------ props is used only to send data form parent to child component where state can show the chaanges dynammiccly to the UI

3-What does the useState hook do, and where did you use it in this project?
Ans------Basically useState give use to variable to manage changing data to ay  project. For example, I used it to store and update tech count nummmmber and update is when button is pressed

4-What does the useEffect hook do, and why did you need it to load the JSON data?
Ans-----I used it to fetch/load the JSON data when the component first loaded, so the data could be displayed in the UI.

5-Why does every item in a .map() list need a unique key prop?
Ans-----The key prop gives each list item a unique identity,like index number which i use in this DevStack projecct so react can easly find it and render it

6-What is conditional rendering? Show one place you used it (example: the empty stack message).
Ans-----Conditional rendering is when we give some condision through code to render some thing when some specific condison was fullfilled...In this project i used i only is the emty stack message in this project .

7-How do you pass data from a parent component to a child component, and how does a child send something back to the parent?
Ans-----We pass data from parent to child using props. But child component can't send data to parent its the boundary of the reaact
